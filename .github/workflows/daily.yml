name: Daily arXiv Report

on:
  schedule:
    # 北京时间凌晨 02:23（= 前一天 UTC 18:23）跑，早起即见昨日最新论文
    # 注：GitHub cron 用 UTC；选非整点分钟避开高峰延迟
    - cron: "23 18 * * *"
  workflow_dispatch: {}   # 支持手动触发

permissions:
  contents: write

concurrency:
  group: daily-arxiv
  cancel-in-progress: false

jobs:
  report:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install deps
        run: pip install pyyaml

      - name: Restore LLM eval cache
        uses: actions/cache@v4
        with:
          path: .cache/llm_evals.json
          key: llm-evals-${{ github.run_id }}
          restore-keys: |
            llm-evals-

      - name: Run daily collector
        env:
          LLM_API_BASE: ${{ secrets.LLM_API_BASE }}
          LLM_API_KEY: ${{ secrets.LLM_API_KEY }}
          LLM_MODEL: ${{ secrets.LLM_MODEL }}
          S2_API_KEY: ${{ secrets.S2_API_KEY }}
        run: python3 daily_arxiv.py --days 3

      - name: Update README recent list
        run: python3 update_readme.py

      - name: Commit and push
        run: |
          git config user.name "collectpapers-bot"
          git config user.email "actions@github.com"
          git add daily/ README.md .cache/daily_seen.json .cache/llm_evals.json 2>/dev/null || true
          if git diff --cached --quiet; then
            echo "No changes, skip commit"
            exit 0
          fi
          git commit -m "docs(daily): arXiv report $(date -u +%F)"
          # 未跟踪的 .cache HTTP 缓存文件会阻塞 rebase —— 先 stash 再拉
          git stash --include-untracked -q 2>/dev/null || true
          if git pull --rebase origin main; then
            git stash pop -q 2>/dev/null || true
            git push
          else
            echo "::error::daily push failed after rebase"
            exit 1
          fi
